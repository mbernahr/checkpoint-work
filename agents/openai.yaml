interface:
  display_name: "Checkpoint Work"
  short_description: "Checkpoint work with a usage reserve"
  default_prompt: "Use $checkpoint-work 10 to complete this task in verified checkpoints while preserving 10% of remaining usage."

policy:
  allow_implicit_invocation: false
